{
  "version": 2,
  "builds": [
    { "src": "api/index.go", "use": "@vercel/go" }
  ],
  "routes": [
    { "src": "/opda/(.*)", "dest": "/api/index" },
    { "src": "/uploads/(.*)", "dest": "/api/index" },
    { "src": "/(.*)", "dest": "/api/index" }
  ]
}
